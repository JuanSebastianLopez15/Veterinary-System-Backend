info:
  name: REENVIO CODIGO
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:3000/api/v1/auth/resend-verification
  body:
    type: json
    data: |-
      {
        "correo":"avillxaneda@bhcore.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
